version: "3"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9000:9000"
    volumes:
      - .:/app
    networks:
      - skill-climbing-shared-network

networks:
  skill-climbing-shared-network:
    external: true